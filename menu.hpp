#ifndef MENU_H
#define MENU_H

#include <SFML/Graphics.hpp>

void draw_title(sf::RenderWindow*, sf::Font);
int title_screen(sf::RenderWindow*, sf::Font);
int level_select(sf::RenderWindow*);

#endif //BUTTON_H